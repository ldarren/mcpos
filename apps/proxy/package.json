{
  "name": "mcpos-sandbox-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sandbox proxy server for secure iframe hosting",
  "scripts": {
    "build": "tsc --noEmit && cross-env INPUT=sandbox.html vite build",
    "watch": "cross-env INPUT=sandbox.html vite build --watch",
    "serve": "tsx server.ts",
    "start": "cross-env NODE_ENV=development npm run build && npm run serve",
    "dev": "cross-env NODE_ENV=development concurrently 'npm run watch' 'npm run serve'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.0",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
